https://news.ycombinator.com/item?id=12093182
https://www.djangoproject.com/download/#supported-versions
https://groups.google.com/forum/#!forum/django-users
	sudo -H pip install Django==2.1
Verify
	import django
	print(django.get_version())
Start a project
	django-admin startproject mysite
Run dev server
	python manage.py runserver
Direct download
	# wget https://www.djangoproject.com/download/1.6/tarball/
	# tar xf Django-1.6.tar.gz
	# cd Django-1.6
x	# python setup.py install

Install with git
	# git clone https://github.com/django/django.git django
Now with pip
x	# pip install -e django/
	# cd django
x	# python setup.py install
Install with subversion (see subversion.txt)
	# cd
	# mkdir django && cd django
	# svn co http://code.djangoproject.com/svn/django/trunk/
	# cd trunk
x	# python setup.py install
Finally, there is one last component required for running the Python FastCGI manager:
the flup library. This provides the actual FastCGI protocol implementation. For Red
Hat-based systems and other systems using Yum as the package manager (EPEL
repositories must be enabled, otherwise you will need to build from source), use:
	# yum install python-flup
For Ubuntu, Debian, and other systems that use Apt or Aptitude, use:
	# apt-get install python-flup

Starting the FastCGI process manager:
You will find a "manage.py" Python script that comes with the default project template.
Move to the directory of this file, and run the following command:
	# cd www
	# python manage.py runfcgi method=prefork host=127.0.0.1 port=9000 pidfile=/var/run/ django.pid

http://losapuntesdetux.blogspot.com.ar/2013/12/django-framework-escrito-en-python-para.html
http://losapuntesdetux.blogspot.com.ar/2013/12/django-framework-escrito-en-python-para_24.html

Database support
https://docs.djangoproject.com/en/2.1/topics/install/#database-installation
https://docs.djangoproject.com/en/2.1/ref/databases/#mysql-notes
https://pypi.org/project/mysqlclient/
	sudo -H pip install mysqlclient
Create database
	CREATE DATABASE <dbname> CHARACTER SET utf8;
mysql timezone
https://dev.mysql.com/doc/refman/8.0/en/mysql-tzinfo-to-sql.html

Using timezone
https://docs.djangoproject.com/en/2.1/topics/i18n/timezones/
